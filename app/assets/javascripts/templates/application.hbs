<div class="navbar navbar-inverse" role="navigation">
	<div class="container-fluid">
	 <!-- Brand and toggle get grouped for better mobile display -->
	 <div class="navbar-header">
	   <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
	     <span class="sr-only">Toggle navigation</span>
	     <span class="icon-bar"></span>
	     <span class="icon-bar"></span>
	     <span class="icon-bar"></span>
	   </button>
	   <ul class="navbar-header">
	   		<li><img src="https://s3.amazonaws.com/vinsync/navbar-logo.png" class="navbar-brand"/></li>
	   </ul>
	 </div>

	 <!-- Collect the nav links, forms, and other content for toggling -->
	 <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
	   <ul class="nav navbar-nav">
	 		{{#view "dropdown"}}
	 			{{#link-to (query-params kind='synth') class="dropdown-toggle"}}Synthesizers<span class="caret"></span>{{/link-to}}
	 			{{#if isSynthDropdownVisible}}	
	 				<ul class="dropdown-list">
	 				  {{#each company in navCompanies}}
	 				    {{#link-to 'companies.show' company tagName="li"}}{{company.name}}{{/link-to}}
	 				  {{/each}}
	 				</ul>
	 			{{/if}}
	 		{{/view}}
	 		{{#view "dropdown"}}
	 			{{#link-to (query-params kind='drum') class="dropdown-toggle"}}Drum Machine<span class="caret"></span>{{/link-to}}
	 			{{#if isDrumDropdownVisible}}	
	 				<ul class="dropdown-list">
	 				  {{#each company in navCompanies}}
	 				    {{#link-to 'companies.show' company tagName="li"}}{{company.name}}{{/link-to}}
	 				  {{/each}}
	 				</ul>
	 			{{/if}}
	 		{{/view}}
	 		{{#view "dropdown"}}
	 			{{#link-to (query-params kind='sampler') class="dropdown-toggle"}}Sampler<span class="caret"></span>{{/link-to}}
	 			{{#if isSamplerDropdownVisible}}	
	 				<ul class="dropdown-list">
	 				  {{#each company in navCompanies}}
	 				    {{#link-to 'companies.show' company tagName="li"}}{{company.name}}{{/link-to}}
	 				  {{/each}}
	 				</ul>
	 			{{/if}}
	 		{{/view}}
	   	</ul>
	 	</div><!-- /.navbar-collapse -->
	</div><!-- /.container-fluid -->
</div>
<div class="container-fluid">
	{{#view "body"}}
	<div class="row-fluid">
		{{outlet}}
	</div>
	{{/view}}
</div>